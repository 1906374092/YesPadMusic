import 'package:flutter/material.dart';

class MusicStorePage extends StatelessWidget {
  const MusicStorePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('musicstore'),
    );
  }
}
