import 'package:flutter/material.dart';

class PhoneLoginComponent extends StatelessWidget {
  const PhoneLoginComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
